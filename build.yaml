targets:
  $default:
    builders:
      drift_dev:
        generate_for:
          - lib/core/database/**
      json_serializable:
        options:
          explicit_to_json: true
        generate_for:
          exclude:
            - test/**
